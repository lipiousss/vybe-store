node_modules
dist
build

.env
backend/.env
frontend/.env

.DS_Store
Thumbs.db

# Uploaded project media is intentionally tracked for the diploma demo.
# Keep product images, avatars, artworks, and site assets in git.

# Database dumps can be committed when exported into the project.
!backend/database-dumps/
!backend/database-dumps/**

.vscode
