node_modules
dist
build

.env
.env.production
backend/.env
frontend/.env

.DS_Store
Thumbs.db

# Uploaded project media is intentionally tracked for the diploma demo.
# Keep product images, avatars, artworks, and site assets in git.

# Database dumps are local-only. Keep backups outside git because GitHub
# rejects large dump archives and SQL backups may contain private data.
backend/database-dumps/*
!backend/database-dumps/.gitkeep

.vscode
.codex/
